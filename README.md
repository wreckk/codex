# codex

Minimal starter project prepared for GitHub + Vercel.

## Local structure

- `index.html` - static landing page
- `styles.css` - page styling
- `vercel.json` - Vercel static routing config

## Deploy

Import this repository into Vercel, or deploy it directly with the Vercel tools/CLI.
